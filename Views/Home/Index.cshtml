@{
    ViewData["Title"] = "B RAJ TRADERS";
}

<!-- ===== Ultra-Luxury Modern Corporate Palette & Innovative UI ===== -->
<style>
    :root {
        /* Premium Luxury Palette (Gold & Slate Navy) */
        --brt-navy: #0D131F;
        --brt-navy-light: #182232;
        --brt-navy-surface: rgba(13, 19, 31, 0.75);
        --brt-gold: #D4AF37;
        --brt-gold-light: #F3E5AB;
        --brt-gold-glow: rgba(212, 175, 55, 0.25);
        --brt-gold-subtle: rgba(212, 175, 55, 0.08);
        --brt-card-bg: #FFFFFF;
        
        /* Glassmorphism Variables */
        --brt-glass-border: rgba(212, 175, 55, 0.2);
        --brt-glass-bg: rgba(13, 19, 31, 0.85);
    }

    /* Keyframe Animations (Razor Escaped @@) */
    @@keyframes floatIcon {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        50% { transform: translateY(-6px) rotate(1deg); }
    }
    @@keyframes pulseGlow {
        0% { box-shadow: 0 0 0 0 rgba(212, 175, 55, 0.4); }
        70% { box-shadow: 0 0 0 12px rgba(212, 175, 55, 0); }
        100% { box-shadow: 0 0 0 0 rgba(212, 175, 55, 0); }
    }
    @@keyframes shimmerSweep {
        0% { left: -150%; }
        100% { left: 250%; }
    }

    /* Flex Grid Alignment */
    .row-equal-height {
        display: flex;
        flex-wrap: wrap;
    }
    .row-equal-height > [class*='col-'] {
        display: flex;
        flex-direction: column;
    }

    /* Next-Gen Innovative Card */
    .innovative-card {
        background: var(--brt-card-bg);
        border: 1px solid rgba(13, 19, 31, 0.08);
        border-radius: 20px;
        transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        position: relative;
        overflow: hidden;
        width: 100%;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }
    .innovative-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: -150%;
        width: 60%;
        height: 100%;
        background: linear-gradient(
            90deg, 
            transparent 0%, 
            rgba(255, 255, 255, 0.7) 50%, 
            transparent 100%
        );
        z-index: 3;
        pointer-events: none;
    }
    .innovative-card:hover::before {
        animation: shimmerSweep 0.8s ease-in-out forwards;
    }
    .innovative-card:hover {
        transform: translateY(-8px);
        border-color: var(--brt-gold) !important;
        box-shadow: 
            0 20px 35px -10px rgba(13, 19, 31, 0.08),
            0 0 20px var(--brt-gold-glow) !important;
    }

    /* Dynamic Hero Bento Card */
    .portfolio-card-hero {
        background: var(--brt-navy);
        border-radius: 24px;
        transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        position: relative;
        overflow: hidden;
        z-index: 1;
        width: 100%;
        min-height: 280px;
        border: 1px solid var(--brt-glass-border);
    }
    .portfolio-card-hero::after {
        content: '';
        position: absolute;
        inset: 0;
        background: 
            radial-gradient(circle at top right, rgba(212, 175, 55, 0.15), transparent 60%),
            radial-gradient(circle at bottom left, rgba(24, 34, 50, 0.8), transparent 60%);
        z-index: 0;
    }
    .portfolio-card-hero:hover {
        transform: translateY(-6px);
        border-color: var(--brt-gold);
        box-shadow: 0 25px 50px -12px rgba(13, 19, 31, 0.35);
    }
    .portfolio-card-hero:hover .arrow-icon {
        transform: translateX(6px);
        color: var(--brt-gold) !important;
    }
    .arrow-icon {
        transition: transform 0.3s ease, color 0.3s ease;
    }

    /* Image Zoom Frame */
    .img-zoom-container {
        overflow: hidden;
        border-radius: 16px;
        position: relative;
    }
    .img-zoom-container img {
        transition: transform 0.6s cubic-bezier(0.2, 0.8, 0.2, 1);
        width: 100%;
        object-fit: cover;
    }
    .innovative-card:hover .img-zoom-container img {
        transform: scale(1.06);
    }

    /* Utility Styling */
    .anim-float-icon {
        animation: floatIcon 4s ease-in-out infinite;
        display: inline-block;
    }
    .pulse-badge {
        animation: pulseGlow 2.5s infinite;
    }
    .text-navy { color: var(--brt-navy) !important; }
    .text-gold { color: var(--brt-gold) !important; }
    .bg-navy { background-color: var(--brt-navy) !important; }
    .bg-gold { background-color: var(--brt-gold) !important; }
    .extra-small { font-size: 0.78rem; letter-spacing: 0.03em; }

    /* Frosted Glass */
    .bg-glass {
        background: var(--brt-glass-bg);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
        border: 1px solid var(--brt-glass-border);
    }

    /* Custom Luxury Buttons */
    .btn-brt-gold {
        background: linear-gradient(135deg, #D4AF37 0%, #C59B27 100%);
        color: #0D131F !important;
        font-weight: 700;
        border: none;
        border-radius: 50px;
        transition: all 0.3s ease;
        box-shadow: 0 6px 18px rgba(212, 175, 55, 0.25);
    }
    .btn-brt-gold:hover {
        background: linear-gradient(135deg, #E2BD45 0%, #D4AF37 100%);
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(212, 175, 55, 0.4);
    }
    .btn-brt-outline {
        border: 1.5px solid var(--brt-navy);
        color: var(--brt-navy) !important;
        font-weight: 600;
        border-radius: 50px;
        transition: all 0.3s ease;
    }
    .btn-brt-outline:hover {
        background: var(--brt-navy);
        color: #FFFFFF !important;
    }
</style>

<!-- ===== Announcement Bar ===== -->
<div class="announcement-bar py-2 text-white" style="background: var(--brt-navy); font-size: 0.85rem; border-bottom: 1px solid rgba(212, 175, 55, 0.15);">
    <div class="container d-flex flex-wrap justify-content-between align-items-center gap-2">
        <div class="announcement-badges d-flex flex-wrap align-items-center gap-3">
            <span class="d-inline-flex align-items-center gap-1.5">
                <span class="material-symbols-outlined fs-6 text-gold">verified</span> Premium Products
            </span>
            <span class="d-inline-flex align-items-center gap-1.5">
                <span class="material-symbols-outlined fs-6 text-gold">support_agent</span> Dedicated Support
            </span>
            <span class="d-inline-flex align-items-center gap-1.5">
                <span class="material-symbols-outlined fs-6 text-gold">workspace_premium</span> Trusted Business Partner
            </span>
        </div>
        <a href="tel:9865680694" class="text-white text-decoration-none fw-semibold d-inline-flex align-items-center gap-1.5">
            <span class="material-symbols-outlined fs-6 text-gold">call</span> +91 98656 80694
        </a>
    </div>
</div>

<!-- ===== Ultra-Clean & Luxury Hero Section ===== -->
<section class="hero-modern-premium position-relative py-5 overflow-hidden" style="font-family: 'Plus Jakarta Sans', sans-serif; background: radial-gradient(circle at 15% 20%, rgba(212, 175, 55, 0.06) 0%, transparent 45%), linear-gradient(135deg, #FAFBFD 0%, #F1F5F9 100%); min-height: 520px;">
    <div class="hero-inner container py-lg-4 position-relative z-1">
        <div class="row align-items-center g-4 g-lg-5">
            
            <!-- Left Column: Content -->
            <div class="col-lg-6">
                <!-- Trust Badge -->
                <div class="d-inline-flex align-items-center gap-2 px-3 py-1.5 rounded-pill mb-3 border bg-white shadow-sm" style="border-color: rgba(212, 175, 55, 0.3) !important;">
                    <span class="material-symbols-outlined fs-6 text-gold">verified_user</span> 
                    <span class="fw-bold extra-small text-uppercase tracking-wider text-navy" style="font-size: 0.72rem;">Trusted Wholesale Mandi Since 2000</span>
                </div>

                <!-- Title -->
                <h1 class="display-4 fw-bold mb-2 text-navy" style="font-family: 'Playfair Display', serif; letter-spacing: -0.5px;">
                    B RAJ <span class="text-gold" style="font-style: italic;">TRADERS</span>
                </h1>

                <!-- Tagline -->
                <p class="hero-tagline fs-5 text-muted mb-4 fw-normal" style="line-height: 1.6;">
                    Garlic &amp; Grocery Commission Mandi<br class="d-none d-md-block" /> 
                    <span class="fw-semibold text-dark">Premium Dealer-to-Dealer Bulk Wholesale Supplier</span>
                </p>

                <!-- Quote Block -->
                <div class="p-3 rounded-end-3 shadow-sm my-4" style="font-family: 'Playfair Display', serif; background: rgba(255, 255, 255, 0.85); backdrop-filter: blur(8px); border-left: 4px solid var(--brt-gold) !important;">
                    <p class="m-0 fs-5 text-secondary fst-italic">
                        <span class="text-gold fs-3 align-middle me-1">&ldquo;</span>Purity in every clove, trust in every deal.<span class="text-gold fs-3 align-middle ms-1">&rdquo;</span>
                    </p>
                </div>

                <!-- CTA Buttons -->
                <div class="hero-cta d-flex flex-wrap gap-3 mb-4">
                    <a asp-controller="Order" asp-action="Request" class="btn btn-brt-gold px-4 py-3 shadow-sm d-inline-flex align-items-center gap-2">
                        <span>Secure Your Bulk Supply</span>
                        <span class="material-symbols-outlined fs-6">arrow_forward</span>
                    </a>
                    <a asp-controller="Catalog" asp-action="Index" class="btn btn-brt-outline px-4 py-3">
                        Explore Catalog
                    </a>
                </div>

                <!-- Inline Stats -->
                <div class="hero-inline-stats d-flex align-items-center gap-4 gap-md-5 pt-4 border-top border-secondary border-opacity-10">
                    <div>
                        <div class="fw-bold fs-2 text-navy" style="font-family: 'Playfair Display', serif;">25+</div>
                        <div class="stat-label text-muted extra-small text-uppercase tracking-wider">Years Legacy</div>
                    </div>
                    <div class="border-end opacity-25 border-dark" style="height: 35px;"></div>
                    <div>
                        <div class="fw-bold fs-2 text-navy" style="font-family: 'Playfair Display', serif;">500+</div>
                        <div class="stat-label text-muted extra-small text-uppercase tracking-wider">Partner Dealers</div>
                    </div>
                    <div class="border-end opacity-25 border-dark" style="height: 35px;"></div>
                    <div>
                        <div class="fw-bold fs-2 text-navy" style="font-family: 'Playfair Display', serif;">30+</div>
                        <div class="stat-label text-muted extra-small text-uppercase tracking-wider">Product Varieties</div>
                    </div>
                </div>
            </div>

            <!-- Right Column: Visual Showcase -->
            <div class="col-lg-6 text-center position-relative">
                <div class="position-relative d-inline-block z-1">
                    <!-- Image Card with Glass Border -->
                    <div class="p-2 rounded-5 shadow-lg bg-white" style="border: 1px solid rgba(212, 175, 55, 0.2);">
                        <img src="~/images/hero.png" alt="Garlic Wholesale Mandi" class="img-fluid rounded-4 object-fit-cover" style="max-height: 440px; width: 100%;" onerror="this.onerror=null; this.src='https://images.unsplash.com/photo-1618160702438-9b02ab6515c9?auto=format&fit=crop&w=800&q=80';" />
                    </div>

                    <!-- Top Left Glass Floating Badge -->
                    <div class="position-absolute top-0 start-0 translate-middle-y p-3 rounded-4 shadow-sm d-flex align-items-center gap-3 d-none d-sm-flex" style="background: rgba(255, 255, 255, 0.92); backdrop-filter: blur(12px); border: 1px solid rgba(212, 175, 55, 0.25); z-index: 2; margin-top: 20px;">
                        <span class="material-symbols-outlined fs-3 text-gold">workspace_premium</span>
                        <div class="text-start">
                            <span class="d-block fw-bold text-dark extra-small">100% Quality Assurance</span>
                            <span class="d-block text-muted" style="font-size:0.72rem;">Direct Mandi Fresh</span>
                        </div>
                    </div>

                    <!-- Bottom Right Dark Floating Badge -->
                    <div class="position-absolute bottom-0 end-0 translate-middle-y p-3 rounded-4 shadow-lg d-flex align-items-center gap-3 d-none d-sm-flex" style="background: rgba(13, 19, 31, 0.92); backdrop-filter: blur(12px); border: 1px solid rgba(212, 175, 55, 0.3); z-index: 2; margin-bottom: 20px;">
                        <span class="material-symbols-outlined fs-3 text-gold">local_shipping</span>
                        <div class="text-start">
                            <span class="d-block fw-bold text-white extra-small">Fast Dispatch</span>
                            <span class="d-block text-light opacity-75" style="font-size:0.72rem;">Pan-India Logistics</span>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
</section>

<!-- ===== Featured Products Showcase ===== -->
@{
    var featured = ViewBag.FeaturedProducts as List<BRT.Models.Product>;
}
@if (featured != null && featured.Any())
{
    <section class="container my-5 py-4">
        <div class="text-center mb-5 fade-in-up">
            <span class="hero-eyebrow fw-semibold text-uppercase tracking-wider small d-inline-flex align-items-center gap-1.5 text-gold">
                <span class="material-symbols-outlined fs-6 anim-float-icon">workspace_premium</span> OUR PRODUCTS
            </span>
            <h2 class="fw-bold mt-2 text-navy">Premium Wholesale Selection</h2>
            <p class="text-muted mx-auto" style="max-width:600px">Top-tier mandi produce direct to distributors, hotels, and bulk retailers.</p>
        </div>
        <div class="row g-4 row-equal-height">
            @foreach (var p in featured)
            {
                <div class="col-lg-4 col-md-6 fade-in-up">
                    <div class="innovative-card p-3 d-flex flex-column justify-content-between h-100">
                        <div>
                            <div class="img-zoom-container bg-light text-center py-4 px-3 position-relative d-flex align-items-center justify-content-center" style="min-height: 200px; background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);">
                                @if (!string.IsNullOrEmpty(p.ImageUrl))
                                {
                                    <img src="@p.ImageUrl" alt="@p.Name" class="img-fluid object-fit-contain" style="max-height: 170px;" />
                                }
                                else
                                {
                                    <div class="rounded-circle p-4 bg-white shadow-sm text-navy anim-float-icon">
                                        <span class="material-symbols-outlined display-4">
                                            @(p.SubCategory?.Category?.Type == BRT.Models.CategoryType.Garlic ? "eco" : "shopping_cart_checkout")
                                        </span>
                                    </div>
                                }
                                <span class="badge position-absolute top-0 start-0 m-3 px-3 py-2 rounded-pill shadow-sm bg-navy text-white" style="font-size: 0.72rem; border: 1px solid rgba(212,175,55,0.3);">
                                    @(p.SubCategory?.Name ?? "General")
                                </span>
                            </div>
                            <div class="pt-3 px-2">
                                <h4 class="card-title h5 fw-bold mb-1 text-navy">
                                    <span>@p.Name</span>
                                </h4>
                                @if (!string.IsNullOrEmpty(p.NameTamil))
                                {
                                    <span class="d-block text-muted small fw-medium mb-2">@p.NameTamil</span>
                                }
                                <p class="card-text text-muted small lh-base">
                                    @(string.IsNullOrEmpty(p.Description) ? "Standard commercial bulk packaging available with fresh mandi stock." : p.Description)
                                </p>
                            </div>
                        </div>
                        <div class="pt-3 px-2 mt-3 border-top d-flex justify-content-between align-items-center">
                            <span class="badge bg-light text-dark border px-2-5 py-1 rounded-pill extra-small fw-semibold">
                                Fresh Stock
                            </span>
                            <a asp-controller="Catalog" asp-action="Product" asp-route-slug="@p.Slug" class="btn btn-sm btn-outline-dark rounded-pill px-3 fw-semibold d-inline-flex align-items-center gap-1">
                                Details <span class="material-symbols-outlined fs-6 arrow-icon">arrow_forward</span>
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
        <div class="text-center mt-5 fade-in-up">
            <a asp-controller="Catalog" asp-action="Index" class="btn btn-brt-outline px-4 py-2-5 shadow-sm fw-semibold">View Full Catalog</a>
        </div>
    </section>
}

<!-- ===== Live Market Dashboard ===== -->
@{
    var dashboard = ViewBag.MarketDashboard as List<BRT.Models.MarketPrice>;
}
<section class="container my-5 py-4">
    <div class="p-4 p-md-5 rounded-4 shadow-sm border fade-in-up" style="background: linear-gradient(180deg, #ffffff 0%, #f8fafc 100%); border-color: rgba(212, 175, 55, 0.15) !important;">
        <div class="text-center mb-5">
            <span class="hero-eyebrow fw-semibold text-uppercase tracking-wider small d-inline-flex align-items-center gap-1.5 text-gold">
                <span class="material-symbols-outlined fs-6 anim-float-icon">trending_up</span> LIVE MARKET
            </span>
            <h2 class="fw-bold mt-2 text-navy">Today's Mandi Wholesale Rates</h2>
            <p class="text-muted mx-auto mb-0" style="max-width:600px">Daily updated rates directly synchronized with mandi markets.</p>
        </div>
        <div class="row g-3 row-equal-height">
            @if (dashboard != null && dashboard.Any())
            {
                foreach (var m in dashboard)
                {
                    <div class="col-lg-3 col-md-4 col-6">
                        <div class="innovative-card p-3 h-100 d-flex flex-column justify-content-between">
                            <div>
                                <div class="d-flex align-items-center justify-content-between mb-2">
                                    <span class="p-2 rounded-circle bg-light text-gold d-inline-flex align-items-center anim-float-icon">
                                        <span class="material-symbols-outlined fs-5">
                                            @(m.Product?.SubCategory?.Category?.Type == BRT.Models.CategoryType.Garlic ? "eco" : "shopping_cart_checkout")
                                        </span>
                                    </span>
                                    <span class="badge bg-danger text-white extra-small fw-semibold pulse-badge">LIVE</span>
                                </div>
                                <h6 class="fw-bold mb-0 text-navy text-truncate">@m.Product?.Name</h6>
                                <span class="text-muted extra-small d-block mb-2">@m.Product?.NameTamil</span>
                            </div>
                            <div class="mt-2 pt-2 border-top">
                                <div class="fs-5 fw-bold text-navy">₹@m.TodayPrice.ToString("N2") <span class="fs-7 text-muted fw-normal">/ unit</span></div>
                                <div class="d-flex justify-content-between align-items-center mt-1 text-muted extra-small">
                                    <span>Updated Today</span>
                                    <span class="material-symbols-outlined text-gold" style="font-size: 0.85rem">verified</span>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            }
            <div class="col-lg-3 col-md-4 col-12">
                <div class="portfolio-card-hero p-4 h-100 text-center d-flex flex-column align-items-center justify-content-center text-white bg-navy">
                    <span class="material-symbols-outlined display-6 mb-2 text-gold anim-float-icon">forum</span>
                    <h6 class="fw-bold mb-1">Need Custom Quotation?</h6>
                    <p class="extra-small mb-3 opacity-75">Connect directly with our desk for custom orders.</p>
                    <a asp-controller="Order" asp-action="Request" class="btn btn-sm btn-brt-gold px-3 py-2 fw-semibold">Get Quotation</a>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ===== Category Portfolio (Bento Grid) ===== -->
<section class="container my-5 py-4">
    <div class="text-center mb-5 fade-in-up">
        <span class="hero-eyebrow fw-semibold text-uppercase tracking-wider small d-inline-flex align-items-center gap-1.5 text-gold">
            <span class="material-symbols-outlined fs-6 anim-float-icon">grid_view</span> PORTFOLIO
        </span>
        <h2 class="fw-bold mt-2 text-navy">Explore Product Portfolio</h2>
        <p class="text-muted mx-auto" style="max-width:600px">Structured solutions tailored for distributors and commercial buyers.</p>
    </div>
    
    <div class="row g-4 row-equal-height">
        <!-- Garlic Mandi Card -->
        <div class="col-lg-6 fade-in-up">
            <a asp-controller="Catalog" asp-action="Garlic" class="portfolio-card-hero p-4 text-white text-decoration-none d-flex flex-column justify-content-between h-100" style="background: linear-gradient(135deg, #0D131F 0%, #182232 100%);">
                <div class="d-flex justify-content-between align-items-start position-relative z-1">
                    <span class="p-3 rounded-4 bg-glass text-gold anim-float-icon">
                        <span class="material-symbols-outlined display-6">eco</span>
                    </span>
                    <span class="badge text-dark fw-bold px-3 py-2 rounded-pill bg-gold">Primary Supply</span>
                </div>
                <div class="mt-4 position-relative z-1">
                    <h3 class="fw-bold text-white mb-2">Garlic Mandi</h3>
                    <p class="small text-light opacity-75 mb-3">All grades of garlic bulbs, cloves, raw, and processed varieties suited for wholesale.</p>
                    <span class="text-gold fw-semibold d-inline-flex align-items-center gap-1">
                        Explore Garlic Section <span class="material-symbols-outlined fs-6 arrow-icon">arrow_forward</span>
                    </span>
                </div>
            </a>
        </div>
        <!-- Grocery Essentials Card -->
        <div class="col-lg-6 fade-in-up">
            <a asp-controller="Catalog" asp-action="Grocery" class="portfolio-card-hero p-4 text-white text-decoration-none d-flex flex-column justify-content-between h-100" style="background: linear-gradient(135deg, #182232 0%, #253347 100%);">
                <div class="d-flex justify-content-between align-items-start position-relative z-1">
                    <span class="p-3 rounded-4 bg-glass text-gold anim-float-icon">
                        <span class="material-symbols-outlined display-6">shopping_cart_checkout</span>
                    </span>
                    <span class="badge bg-light text-dark fw-bold px-3 py-2 rounded-pill">Grocery Staples</span>
                </div>
                <div class="mt-4 position-relative z-1">
                    <h3 class="fw-bold text-white mb-2">Grocery Essentials</h3>
                    <p class="small text-light opacity-75 mb-3">Top quality pulses, spices, rice, and commercial kitchen staples direct from mandi.</p>
                    <span class="text-gold fw-semibold d-inline-flex align-items-center gap-1">
                        Explore Grocery Catalog <span class="material-symbols-outlined fs-6 arrow-icon">arrow_forward</span>
                    </span>
                </div>
            </a>
        </div>
        <!-- Loose Packs Card -->
        <div class="col-lg-6 fade-in-up">
            <a asp-controller="Catalog" asp-action="Loose" class="portfolio-card-hero p-4 text-white text-decoration-none d-flex flex-column justify-content-between h-100" style="background: linear-gradient(135deg, #253347 0%, #0D131F 100%);">
                <div class="d-flex justify-content-between align-items-start position-relative z-1">
                    <span class="p-3 rounded-4 bg-glass text-gold anim-float-icon">
                        <span class="material-symbols-outlined display-6">inventory_2</span>
                    </span>
                    <span class="badge bg-secondary text-white fw-medium px-3 py-1 rounded-pill">Retail Packs</span>
                </div>
                <div class="mt-4 position-relative z-1">
                    <h3 class="fw-bold text-white mb-2">Loose &amp; Custom Packing</h3>
                    <p class="small text-light opacity-75 mb-3">Custom weight packaging for retail stores &amp; direct distribution.</p>
                    <span class="text-gold fw-semibold d-inline-flex align-items-center gap-1">
                        View Packing Options <span class="material-symbols-outlined fs-6 arrow-icon">arrow_forward</span>
                    </span>
                </div>
            </a>
        </div>
        <!-- Bulk Inquiry Card -->
        <div class="col-lg-6 fade-in-up">
            <a asp-controller="Order" asp-action="Request" class="portfolio-card-hero p-4 text-dark text-decoration-none d-flex flex-column justify-content-between h-100" style="background: linear-gradient(135deg, #D4AF37 0%, #C59B27 100%);">
                <div class="d-flex justify-content-between align-items-start position-relative z-1">
                    <span class="p-3 rounded-4 bg-white text-dark anim-float-icon shadow-sm">
                        <span class="material-symbols-outlined display-6">handshake</span>
                    </span>
                    <span class="badge bg-dark text-white fw-medium px-3 py-1 rounded-pill">Direct Mandi</span>
                </div>
                <div class="mt-4 position-relative z-1">
                    <h3 class="fw-bold text-dark mb-2">Bulk Wholesale Inquiry</h3>
                    <p class="small text-dark opacity-75 mb-3">Get daily customized quotation based on volume and transport setup.</p>
                    <span class="text-dark fw-bold d-inline-flex align-items-center gap-1">
                        Request Quote Now <span class="material-symbols-outlined fs-6 arrow-icon">arrow_forward</span>
                    </span>
                </div>
            </a>
        </div>
    </div>
</section>

<!-- ===== Why Choose BRT ===== -->
<section class="container my-5 py-4">
    <div class="row g-5 align-items-center">
        <div class="col-lg-6 fade-in-up">
            <span class="hero-eyebrow fw-semibold text-uppercase tracking-wider small d-inline-flex align-items-center gap-1.5 text-gold">
                <span class="material-symbols-outlined fs-6 anim-float-icon">handshake</span> WHY CHOOSE BRT
            </span>
            <h2 class="fw-bold mt-2 mb-3 text-navy">Trusted Wholesale Partner Since 2000</h2>
            <p class="text-muted mb-4">We supply premium-grade garlic and grocery staples with complete price transparency, reliable nationwide transport, and dedicated support for long-term partners.</p>
            
            <div class="row g-3 mb-4">
                <div class="col-6">
                    <div class="d-flex align-items-center gap-2">
                        <span class="material-symbols-outlined text-gold fs-5">check_circle</span>
                        <span class="fw-medium text-dark small">Premium Grade Quality</span>
                    </div>
                </div>
                <div class="col-6">
                    <div class="d-flex align-items-center gap-2">
                        <span class="material-symbols-outlined text-gold fs-5">check_circle</span>
                        <span class="fw-medium text-dark small">Live Market Rates</span>
                    </div>
                </div>
                <div class="col-6">
                    <div class="d-flex align-items-center gap-2">
                        <span class="material-symbols-outlined text-gold fs-5">check_circle</span>
                        <span class="fw-medium text-dark small">Direct Mandi Sourcing</span>
                    </div>
                </div>
                <div class="col-6">
                    <div class="d-flex align-items-center gap-2">
                        <span class="material-symbols-outlined text-gold fs-5">check_circle</span>
                        <span class="fw-medium text-dark small">Fast Dispatch Network</span>
                    </div>
                </div>
            </div>
            
            <a asp-controller="Home" asp-action="WhyChooseUs" class="btn btn-brt-outline px-4 fw-semibold">Learn More About Us</a>
        </div>
        
        <div class="col-lg-6">
            <div class="row g-3 row-equal-height">
                <div class="col-6 fade-in-up">
                    <div class="innovative-card text-center p-4 h-100">
                        <span class="material-symbols-outlined display-6 text-gold mb-2 anim-float-icon">workspace_premium</span>
                        <h6 class="fw-bold text-navy">Strict Grading</h6>
                        <p class="text-muted extra-small mb-0">Consistently graded for uniform size &amp; purity</p>
                    </div>
                </div>
                <div class="col-6 fade-in-up">
                    <div class="innovative-card text-center p-4 h-100">
                        <span class="material-symbols-outlined display-6 text-gold mb-2 anim-float-icon">local_shipping</span>
                        <h6 class="fw-bold text-navy">Safe Transport</h6>
                        <p class="text-muted extra-small mb-0">Moisture-proof packing &amp; door delivery</p>
                    </div>
                </div>
                <div class="col-6 fade-in-up">
                    <div class="innovative-card text-center p-4 h-100">
                        <span class="material-symbols-outlined display-6 text-gold mb-2 anim-float-icon">trending_up</span>
                        <h6 class="fw-bold text-navy">Fair Pricing</h6>
                        <p class="text-muted extra-small mb-0">Real-time daily competitive market prices</p>
                    </div>
                </div>
                <div class="col-6 fade-in-up">
                    <div class="innovative-card text-center p-4 h-100">
                        <span class="material-symbols-outlined display-6 text-gold mb-2 anim-float-icon">support_agent</span>
                        <h6 class="fw-bold text-navy">24/7 Desk</h6>
                        <p class="text-muted extra-small mb-0">Instant response via call or WhatsApp</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ===== Industries We Serve ===== -->
<section class="container my-5 py-4">
    <div class="text-center mb-5 fade-in-up">
        <span class="hero-eyebrow fw-semibold text-uppercase tracking-wider small d-inline-flex align-items-center gap-1.5 text-gold">
            <span class="material-symbols-outlined fs-6 anim-float-icon">domain</span> INDUSTRIES WE SERVE
        </span>
        <h2 class="fw-bold mt-2 text-navy">Commercial Clients &amp; Partners</h2>
    </div>
    <div class="row g-3 text-center row-equal-height">
        @{
            var industries = new[] {
                (Icon: "storefront", Name: "Supermarkets"),
                (Icon: "shopping_cart_checkout", Name: "Grocery Mandis"),
                (Icon: "local_mall", Name: "Retail Chains"),
                (Icon: "hotel", Name: "Hotels & Resorts"),
                (Icon: "flatware", Name: "Restaurants"),
                (Icon: "soup_kitchen", Name: "Catering Teams"),
                (Icon: "public", Name: "Exporters"),
                (Icon: "location_on", Name: "Pan-India Network")
            };
        }
        @foreach (var ind in industries)
        {
            <div class="col-6 col-md-3 fade-in-up">
                <div class="innovative-card p-3 d-flex flex-column align-items-center justify-content-center h-100">
                    <span class="material-symbols-outlined text-navy fs-3 mb-2 anim-float-icon">@ind.Icon</span>
                    <h6 class="fw-semibold mb-0 text-dark small">@ind.Name</h6>
                </div>
            </div>
        }
    </div>
</section>

<!-- ===== Footer Disclaimer ===== -->
<section class="container my-5 py-3">
    <div class="innovative-card p-3 text-center d-flex flex-wrap justify-content-center align-items-center gap-3 gap-md-4 text-muted small fade-in-up">
        <span class="d-inline-flex align-items-center gap-1.5"><span class="material-symbols-outlined text-gold fs-6">check_circle</span> Bulk Orders Preferred</span>
        <span class="d-inline-flex align-items-center gap-1.5"><span class="material-symbols-outlined text-gold fs-6">check_circle</span> Garlic Wholesale</span>
        <span class="d-inline-flex align-items-center gap-1.5"><span class="material-symbols-outlined text-gold fs-6">check_circle</span> Grocery Wholesale</span>
        <span class="d-inline-flex align-items-center gap-1.5"><span class="material-symbols-outlined text-gold fs-6">check_circle</span> Live Daily Market Rates</span>
        <span class="d-inline-flex align-items-center gap-1.5"><span class="material-symbols-outlined text-gold fs-6">check_circle</span> Tax/GST Included</span>
    </div>
</section>
